https://www.kaggle.com/datasets/palashfendarkar/wa-fnusec-telcocustomerchurn



## after adding file if want to remove:

-- create .gitignore and add  folder : churn_analysis/ or file: churn_analysis.txt
-- remove filr or folder from cache: 
                git rm --cached file_to_ignore.txt
                git rm --cached -r folder_to_ignore/

-- see the status: git status
-- git commit -m "add comment"
-- git branch -M main if newly created repository or else directly next step
-- git push -u origin main
